def shell_sort(vector):
    contador_sublista = len(vector) // 2
    while contador_sublista > 0:
        for i in range(contador_sublista):
            
    return vector

def quick_sort(vector):
    return vector

def merge_sort(vector):
    return vector